#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Full lint across the monorepo
echo "Running lint..."
pnpm lint

# Ensure type-check passes before push
echo "Running type-check..."
pnpm type-check

echo "Running tests..."
pnpm test

echo "✓ Pre-push checks passed"
